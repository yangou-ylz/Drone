# 树莓派 × STM32 飞控正式对接文档

**版本**：v1.4 · 2026-07-27
**对象**：树莓派侧开发人员 / 树莓派侧 AI 代码生成器
**飞控硬件**：凌霄飞控 = STM32F407 可编程主控 + 凌霄 IMU 闭源控制核心

> 本文档可以直接发给树莓派侧开发人员。请同时附上 `groundTest/ano_protocol.py`、`groundTest/send_f5.py`、`groundTest/test_f5_frame.py`，这样对方可以直接复用构帧、校验和日志监听逻辑。

---

## 1. 系统边界

```
树莓派
  ├─ N10P/SLAM：输出当前定位
  ├─ K230/视觉：输出目标相对偏移
  ├─ 坐标转换：转成飞控坐标系，单位 cm
  └─ 串口发送：0xF5 位置/目标帧，建议 20~50Hz

STM32F407
  ├─ UART2 接收 0xF5
  ├─ 第一阶段：只解析、保存快照、输出 0xA0 日志
  ├─ GUI调试：把0xF5快照镜像成0xF6，经IMU/数传给上位机
  └─ 后续阶段：在确认通信/坐标/失效保护后，再接 PID 和 0x41

凌霄 IMU
  └─ 闭源传感/融合/控制核心，不修改内部代码
```

分工：

| 模块 | 负责 | 不负责 |
|---|---|---|
| 树莓派 | SLAM、视觉、坐标转换、0xF5 打包发送、树莓派侧日志 | PID 控制、飞控决策 |
| STM32 | 解析 0xF5、日志、后续 PID 和速度指令组织 | SLAM/图像处理、修改 IMU 闭源算法 |
| 凌霄 IMU | 闭源速度环/姿态环/融合控制 | 树莓派应用逻辑 |

当前 STM32 阶段状态：

- `UART2` 已按 `PD5=TX / PD6=RX / 500000 / 8N1 / 无校验 / 无流控` 接入树莓派链路。
- `0xF5` 已进入 UART2 专用接收状态机，校验 `AA 61/FF F5 19`、31B 总长、SC/AC。
- 校验通过后已解析 `cur_x/y/z`、`tar_x/y/z`、`flags`，并通过 UART2 TX 回 `0xA0` 绿色 ACK。
- 树莓派侧已实测固定帧、X/Y/Z 方向帧、`SLAM_VALID/TARGET_VALID` flags 帧均能收到正确 ACK。
- STM32 已新增 `0xF6` 下行镜像调试帧：不改变树莓派 `0xF5`，只把解析后的最近快照经 IMU/数传发给 GUI“位置测试”页面。
- 当前固件仍然 **只收、只解析、只回日志，不控飞**。
- 树莓派侧不能假设发送 `0xF5` 后飞机会运动；运动控制必须等后续阶段逐步打开。
- 2026-07-27 自主起降第一阶段已通过：有桨低高度正常起降、有桨常规中止降落、地面急停上锁均已验收。下一阶段允许接回树莓派 `0xF5` 做只读位置观测和 GUI 状态显示，但仍不允许接位置闭环、XY PID 或 `0x41` 速度控制。
- 2026-07-27 插入 GUI 单次相对位移阶段：新增 `0xF9` 由 GUI 发送 X/Y/Z 相对位移，STM32 复用现有速度积分 PID3D 执行位移。这个阶段 **不读取树莓派 `0xF5` 来控飞**，目的是先把 GUI 指令、自主状态机、飞控内部位置环跑稳，后续再把树莓派位置作为观测接入。

协作调度方式：

- 本项目现在采用 **双端同步阶段闸门**，不是飞控侧或树莓派侧单线推进。
- 每一阶段由飞控侧先完成一个最小可验证改动并汇报当前进度。
- 随后明确树莓派侧必须执行的脚本、期望日志、失败时需要回传的材料。
- 树莓派侧完成并把日志/结果发回后，飞控侧再继续下一阶段。
- 未收到当前阶段树莓派侧通过结果前，不进入后续控制阶段，尤其不跳级接 PID、`0x41` 或实机运动输出。
- 例外：2026-07-26~2026-07-27 的低高度自主起降、以及当前 `0xF9` GUI 单次相对位移，属于 GUI/STM32/IMU/飞控内部位置环验证，不使用树莓派位置数据。后续接回树莓派位置主线时，先只看 `0xF5/F6/F8` 观测，不直接让飞机按树莓派坐标运动。

---

## 2. 串口接线

### 2.1 第一阶段推荐：USB-TTL 接树莓派 USB 口

这是正式联调第一阶段推荐方案，调试最清楚，不需要先处理树莓派 GPIO 串口控制台/蓝牙占用问题。

| USB-TTL 模块 | 方向 | STM32F407 |
|---|---:|---|
| `TXD` | → | `PD6 / UART2_RX` |
| `RXD` | ← | `PD5 / UART2_TX` |
| `GND` | ↔ | `GND` |
| `VCC` | 不接 | 不接 STM32，除非另行确认供电方案 |

要求：

- USB-TTL 必须是 **3.3V TTL 电平**。
- 不要使用 RS232 电平模块。
- 如果 USB-TTL 有 `3.3V/5V` 跳帽，设置为 `3.3V`。
- 必须共地。
- 强烈建议接双向线：`TXD/RXD/GND` 都接上，这样树莓派能看到 STM32 的 `0xA0` ACK/日志。
- **严禁默认使用 DAP/UART 复合烧录板当树莓派 UART2 串口桥。** 本项目已实测：某款 ANO `SWD&UART V2.0` 类 DAP/UART 模块接到树莓派 USB 后，即使树莓派关机，也会导致飞控 `0x0D` 电压数据归零/消失，随后光流、激光高度、通用速度等外部传感数据消失，并出现 `[A0 红] 运动解算失效复位`；拔掉后电压和外部传感恢复，并出现 `[A0 绿] 运动解算启动`。
- 推荐使用已经验证正常的简单 3.3V USB-TTL，或后续使用经验证无反灌/无电平异常的隔离串口模块。任何新串口模块上线前，先只接 `TX/RX/GND`，`VCC/5V` 不接，并验证 `0x0D` 电压和 `0x0E` 外接模块状态稳定。

树莓派侧串口设备：

- 优先使用稳定路径：`/dev/serial/by-id/...`
- 临时测试可用：`/dev/ttyUSB0`
- 串口参数：`500000 baud, 8N1, no parity, no flow control`

### 2.2 备选：树莓派 GPIO UART 直连

如果后续要取消 USB-TTL，可以改成 GPIO UART：

| 树莓派 | 方向 | STM32F407 |
|---|---:|---|
| `GPIO14 / TXD` | → | `PD6 / UART2_RX` |
| `GPIO15 / RXD` | ← | `PD5 / UART2_TX` |
| `GND` | ↔ | `GND` |

树莓派需要额外处理：

- 禁用串口控制台。
- 如使用硬件 UART，必要时禁用蓝牙占用。
- 设备名通常是 `/dev/serial0` 或 `/dev/ttyAMA0`。

---

### 2.3 只读获取 IMU 遥测：USART5 旁路监听，不是控制链路

如果树莓派需要额外读取凌霄 IMU 输出的姿态、速度、高度、电压/状态等数据用于雷达点云建图，可以旁路监听飞控外露的 `USART5`，但**只能只读监听，不能发送**。

飞控板 `USART5 RX/TX` 标注是相对于 STM32：

| 飞控 USART5 引脚 | 实际方向 | 含义 |
|---|---:|---|
| `RX` | IMU → STM32 | STM32 接收凌霄 IMU 数据，线上有姿态/速度/高度等匿名帧 |
| `TX` | STM32 → IMU | STM32 发送遥控、外部传感、实时控制等输入帧给 IMU |

树莓派只读监听接线：

| 飞控/USB-TTL | 接法 |
|---|---|
| 飞控 `USART5 RX` | → USB-TTL `RX` |
| 飞控 `GND` | ↔ USB-TTL `GND` |
| USB-TTL `TX` | 不接 |
| USB-TTL `VCC/5V` | 不接 |
| 飞控 `USART5 TX` | 不接 |

注意：这里的 `RX → RX` 不是写错，因为树莓派是在监听已有的 IMU→STM32 数据线；普通串口 `TX↔RX` 交叉只适用于两个设备点对点通信。现场已实测：USB-TTL `TX` 一旦接入 USART5，会导致 `0x0D` 电压和外部传感数据异常。树莓派主动发送 SLAM/目标位置必须继续走 UART2 的 `0xF5`，不能走 USART5。

---

## 3. 0xF5 帧格式

匿名协议基础格式：

```text
AA | DEST | CMD | LEN | DATA[LEN] | SC | AC
```

`0xF5` 自定义位置帧固定 31 字节：

| 偏移 | 字段 | 类型 | 说明 |
|---:|---|---|---|
| 0 | `HEAD` | u8 | 固定 `0xAA` |
| 1 | `DEST` | u8 | 固定 `0x61`，STM32 本机地址 |
| 2 | `CMD` | u8 | 固定 `0xF5` |
| 3 | `LEN` | u8 | 固定 `0x19`，DATA 长度 25 |
| 4~7 | `cur_x` | s32 LE | 当前 X，cm |
| 8~11 | `cur_y` | s32 LE | 当前 Y，cm |
| 12~15 | `cur_z` | s32 LE | 当前 Z，cm |
| 16~19 | `tar_x` | s32 LE | 目标 X，cm |
| 20~23 | `tar_y` | s32 LE | 目标 Y，cm |
| 24~27 | `tar_z` | s32 LE | 目标 Z，cm |
| 28 | `flags` | u8 | 状态标志 |
| 29 | `SC` | u8 | 和校验 |
| 30 | `AC` | u8 | 附加校验 |

`flags`：

| bit | 名称 | 含义 |
|---:|---|---|
| 0 | `SLAM_VALID` | 1=SLAM 定位正常 |
| 1 | `TARGET_VALID` | 1=目标坐标有效 |
| 2 | `VISUAL_MODE` | 1=目标来自视觉/K230，0=固定航点 |
| 3~7 | 保留 | 必须填 0 |

无效值：

- 协议字节约定为 `0x80000000`。
- Python signed int 里必须写 `-2147483648`。
- 不要把 `0x80000000` 直接传给 `struct.pack("<i")`，否则会越界报错。

校验：

```python
sc = 0
ac = 0
for b in frame[:29]:
    sc = (sc + b) & 0xFF
    ac = (ac + sc) & 0xFF
frame[29] = sc
frame[30] = ac
```

黄金样例：

```text
cur=(0,0,80), tar=(100,0,80), flags=0x03

AA 61 F5 19 00 00 00 00 00 00 00 00 50 00 00 00 64 00 00 00 00 00 00 00 50 00 00 00 03 20 36
```

### 3.1 STM32 下行镜像 `0xF6`

树莓派侧不需要发送 `0xF6`，也不需要修改已经跑通的 `0xF5`。`0xF6` 是 STM32 给 GUI 用的调试镜像：

```text
AA FF F6 25 |
cur_x cur_y cur_z tar_x tar_y tar_z  (6*s32 LE, cm)
flags                                     (u8)
rx_cnt len_err_cnt checksum_err_cnt       (3*u32 LE)
SC AC
```

用途：

- GUI `功能 → 位置测试 → 实时数据` 能看到 STM32 实际解析到的 `cur/tar/flags`，不是树莓派本地打印值。
- 通过 `rx_cnt / len_err_cnt / checksum_err_cnt` 判断是树莓派没发、STM32没收、还是校验/长度错误。
- 镜像最高约 10Hz，只用于调试和坐标对齐，不接 PID、不改变飞控输出。

---

## 4. 坐标系

飞控要求树莓派发来的坐标已经转换到飞控坐标系：

```text
原点：任务启动/对齐时飞机所在位置
X：机头前方为正
Y：机体左侧为正
Z：向上为正
单位：cm
```

树莓派职责：

- 记录 SLAM 初始化时的世界坐标原点。
- 记录起始 yaw / 机头方向。
- 把 SLAM 世界坐标旋转/平移到飞控坐标系。
- 发给 STM32 前必须已经是 `X前、Y左、Z上、cm`。

调试时先只发固定值，不要一上来接真实 SLAM：

```text
cur=(0,0,0), tar=(100,0,0)  # 验证 +X 前
cur=(0,0,0), tar=(0,100,0)  # 验证 +Y 左
cur=(0,0,0), tar=(0,0,100)  # 验证 +Z 上
```

---

## 5. 树莓派侧建议实现

优先直接复用本仓库：

- `groundTest/ano_protocol.py`
- `groundTest/send_f5.py`
- `groundTest/test_f5_frame.py`

核心构帧函数逻辑：

```python
import struct

INVALID_S32 = -2147483648
FLAG_SLAM_VALID = 0x01
FLAG_TARGET_VALID = 0x02
FLAG_VISUAL_MODE = 0x04


def to_s32_cm(v):
    if v is None or (isinstance(v, float) and v != v):
        return INVALID_S32
    iv = int(round(v))
    if not (INVALID_S32 <= iv <= 2147483647):
        raise ValueError(f"out of s32 range: {v}")
    return iv


def build_f5_frame(cur_x, cur_y, cur_z, tar_x, tar_y, tar_z, flags):
    frame = bytearray(31)
    frame[0:4] = bytes([0xAA, 0x61, 0xF5, 0x19])
    struct.pack_into(
        "<iiiiiiB",
        frame,
        4,
        to_s32_cm(cur_x),
        to_s32_cm(cur_y),
        to_s32_cm(cur_z),
        to_s32_cm(tar_x),
        to_s32_cm(tar_y),
        to_s32_cm(tar_z),
        flags & 0xFF,
    )
    sc = 0
    ac = 0
    for b in frame[:29]:
        sc = (sc + b) & 0xFF
        ac = (ac + sc) & 0xFF
    frame[29] = sc
    frame[30] = ac
    return bytes(frame)
```

必须有日志：

- 同时打印到屏幕和文件。
- 文件建议使用 rotating log。
- 每次记录：串口设备名、波特率、TX计数、flags、cur/tar、收到的 STM32 `0xA0` 日志。
- 第一阶段看到 STM32 回传类似：

```text
[RX 0xA0 GREEN] F5 #12 f=03 c=0,0,80 t=100,0,80
```

---

## 6. 分阶段联调计划

执行原则：每个阶段必须“飞控侧完成并汇报 → 树莓派侧按指定命令验证 → 用户回传日志 → 再进入下一阶段”。不要一次性把后续控制目标全部做完。

### 阶段 0：文档和代码版本确认

目标：确认双方拿到的是同一版协议。

验收：

- 树莓派侧运行 `python3 groundTest/test_f5_frame.py`，输出 `OK`。
- 黄金 hex 与本文档一致。
- STM32 侧确认固件包含 `0xF5` 解析入口。

### 阶段 1：串口硬件链路

目标：只验证 USB-TTL 和双向串口。

做法：

- USB-TTL 插树莓派 USB。
- `TXD -> PD6`，`RXD <- PD5`，`GND <-> GND`。
- 树莓派确认 `/dev/serial/by-id/...` 或 `/dev/ttyUSB0`。
- 插上串口桥后先看匿名上位机/GUI/抓包中的 `0x0D` 电池电压和 `0x0E` 外接模块状态，不要先运行控制逻辑。
- 若插上后电压变 0、外部传感无数据或出现 `运动解算失效复位`，立即拔掉该串口桥，换回已验证的简单 3.3V USB-TTL；不要继续在光流/IMU协议层打补丁。

验收：

- 树莓派能打开串口。
- STM32 上电后树莓派脚本能持续监听，不报串口异常。
- 匿名上位机/GUI 能持续看到合理 `0x0D` 电池电压。
- `0x0E` 外接模块状态不因插入 UART2 串口桥而从正常变成无数据。

### 阶段 2：固定 0xF5 单帧

目标：确认 STM32 能收到合法 `0xF5`。

命令：

```bash
python3 groundTest/send_f5.py --port /dev/ttyUSB0 --cur 0,0,80 --tar 100,0,80
```

验收：

- 树莓派日志出现 `[TX #1]`。
- 树莓派收到 STM32 `0xA0`：
  - `F5 #... f=03 c=0,0,80 t=100,0,80`

### 阶段 3：固定坐标方向测试

目标：确认 X/Y/Z 方向不反。

命令：

```bash
python3 groundTest/send_f5.py --port /dev/ttyUSB0 --cur 0,0,0 --tar 100,0,0 --rate 10 --duration 5
python3 groundTest/send_f5.py --port /dev/ttyUSB0 --cur 0,0,0 --tar 0,100,0 --rate 10 --duration 5
python3 groundTest/send_f5.py --port /dev/ttyUSB0 --cur 0,0,0 --tar 0,0,100 --rate 10 --duration 5
```

验收：

- STM32 日志能交替显示当前 `c=` 和目标 `t=`。
- 三个方向的数值和预期一致。
- 这一步仍然不解锁、不控飞。

### 阶段 4：失效状态测试

目标：验证 flags 不会被误解。

命令：

```bash
python3 groundTest/send_f5.py --port /dev/ttyUSB0 --cur 0,0,0 --tar 100,0,0 --slam-invalid
python3 groundTest/send_f5.py --port /dev/ttyUSB0 --cur 0,0,0 --tar 100,0,0 --target-invalid
```

验收：

- STM32 能收到 `f=02` 或 `f=01` 等 flags 变化。
- 当前阶段只记录日志。
- 后续控制阶段必须把 `SLAM_VALID=0` 和超时视为安全停止条件。

### 阶段 5：接入 SLAM 当前坐标

目标：只接 `cur`，目标先等于当前位置。

规则：

- `tar = cur`
- 第一轮真实 SLAM 测试建议 `TARGET_VALID=0`，即 flags 只置 `SLAM_VALID`；也可以 `tar=cur` 作为双保险。
- SLAM 未收敛、定位跳变或丢失时必须清 `SLAM_VALID=0`。
- 不允许触发实际运动。

验收：

- 坐标连续。
- 单位是 cm。
- 静止时噪声可接受。
- 起点附近不会突然出现十几米偏移。
- STM32 ACK 中 `c=` 能连续反映真实 SLAM 当前位置；`t=` 等于 `c=` 或目标无效。

### 阶段 6：目标坐标日志干运行

目标：树莓派发真实 `cur/tar`，STM32 只计算/打印误差，不接 0x41。

验收：

- `tar-cur` 方向正确。
- 视觉丢失时 `tar=cur` 或 `TARGET_VALID=0`。
- SLAM 丢失时 `SLAM_VALID=0`。

### 阶段 7：闭环控制干运行

目标：STM32 内部计算 PID 输出，但不送入飞行输出。

验收：

- 目标在 +X，输出方向应为 +X。
- 目标在 -X，输出方向应为 -X。
- Y/Z 同理。
- 有限幅、有超时、有日志。

### 阶段 8：低速单轴实机

目标：每次只打开一个轴，低速、短距离、带保护。

顺序：

1. X 轴 20cm
2. Y 轴 20cm
3. Z 轴 20cm

验收：

- 方向正确。
- 停止可靠。
- 不明显反向、不发散。

### 阶段 9：XY/Z 联动和视觉任务

目标：单轴稳定后逐步打开组合任务。

顺序：

1. XY 联动
2. 加 Z
3. 固定航点
4. 视觉目标
5. 视觉丢失/SLAM丢失/串口拔掉保护

---

## 7. 故障排查顺序

固定按这个顺序查，不要直接猜控制算法：

1. 电源/电压：插上 UART2 串口桥后 `0x0D` 电池电压是否还存在且合理；若没有电压，先排查串口桥、反灌电、地线、电平和飞控供电线。
2. 外接状态：`0x0E` 中 `STA_G_VEL` / `STA_ALT_ADD` 是否稳定，是否出现 `运动解算失效复位`。
3. 树莓派日志：是否按 500000 打开正确串口。
4. 树莓派 TX：是否发出 31B `AA 61 F5 19 ...`。
5. STM32 ACK：是否收到 `F5 #...` 的 `0xA0`。
6. flags：`SLAM_VALID` / `TARGET_VALID` 是否符合当前状态。
7. 坐标：是否已经转成 `X前、Y左、Z上、cm`。
8. 后续控制阶段再查 PID、限幅、0x41 和飞行模式。

---

## 8. 当前双方 TODO

树莓派侧：

- [x] 使用 USB-TTL 打开 `/dev/serial/by-id/...` 或 `/dev/ttyUSB0`。
- [x] 跑通 `test_f5_frame.py`。
- [x] 跑通固定 `send_f5.py` 并收到 STM32 `0xA0` ACK。
- [x] 跑通 X/Y/Z 固定方向帧 ACK 测试。
- [x] 跑通 `SLAM_VALID/TARGET_VALID` flags 失效 ACK 测试。
- [x] 实现屏幕 + 文件日志。
- [ ] 完成 SLAM 世界坐标到飞控坐标系转换。
- [ ] SLAM 未收敛时清除 `SLAM_VALID`。
- [ ] 视觉/目标无效时清除 `TARGET_VALID` 或令 `tar=cur`。

STM32 侧：

- [x] `0xF5` 进入上行分发。
- [x] `UART2 RX` 从 `NoUse` 改为 `Uplink_Cmd_Uart2RxByte`，PD6 能接收树莓派字节流。
- [x] 解析 `cur/tar/flags` 并保存快照。
- [x] 通过 UART2 TX / PD5 回 `0xA0` 绿色 ACK：`F5 #... f=03 c=... t=...`。
- [x] 当前阶段不接 PID、不改飞行输出。
- [ ] 后续阶段增加超时保护。
- [ ] 后续阶段增加误差/PID干运行日志。
- [ ] 后续阶段在实测通过后再接实际控制输出。
