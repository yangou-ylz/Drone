# 位置测试功能（Position Test）总体规划

> 2026-07-19 注意：本文是旧的 IMU 测试台内“位置测试面板”规划，目标是比较 0x07/0x01/0x32 等位置估计算法。
> 当前新增的主菜单独立“位置测试”页面用于树莓派 `0xF5` 数据的 STM32 下行镜像 `0xF6` 调试，代码在
> `gui/position_test/position_test_window.py`。两者不是同一个功能，不要把本文的“不碰 STM32 C 代码”误套到
> `0xF6` 镜像阶段。

> 目标：在 GUI（`/gui`，**不碰 STM32 C 代码**）里做一个高度抽象、可扩展、易维护的
> 位置调试功能。先在上位机验证各种"由观测量得到位置"的算法，效果好的再移植到飞控。
> 创建日期：2026-07-17

---

## 一、功能需求（来自用户）

1. 底层：一个**高度抽象**的位置估计器体系，接口丰富统一，支持多种"位置来源"：
   - 直接接入外部传感器观测位置（转发，0x08 / 0x32 帧）
   - 速度积分（0x07 速度帧 → 位置）
   - 加速度二次积分（0x01 IMU 帧 → 位置）
   - 未来：滤波、融合（互补滤波、卡尔曼等）
   - 通过**传参 / 改模式**切换底层位置计算方式
2. 算法都遵循**统一抽象接口**，可统一注册、管理、扩展。
   接入新算法后启动 GUI 即刻生效，把观测量处理成位置并显示。
3. GUI 排版：
   - X / Y / Z **各自一个独立图**（不合并），三块长方体区域相互隔开
   - 每个图**下方**对齐显示该轴实时**位移**数值
   - **检测的是位移（相对量），不是绝对位置**
   - 统一「装填」按钮：点击后开始计算一个时间阈值内的相对位移，分显示在三图
   - 「清除/重置」功能
   - 可在界面调判据参数（保持时长、稳定窗口等，参考姿态/线速度面板但不照搬）
   - CSV 导出
   - 其它便于调试底层算法的功能（见待定项）

## 二、约束

- 代码只写在 `/gui`，纯 Python + PySide6 + pyqtgraph，复用 `ImuDataHub` 信号
- 与现有 `velocity_test_panel.py` / `yaw_test_panel.py` 风格一致，但功能不照搬
- 数据源：`ImuDataHub` 的 `velocity`(0x07) / `imu_raw`(0x01) / 位置帧(0x08/0x32) 信号

## 三、待用户确认的关键决策（见下方"方案征询"）

- [ ] 底层架构：策略模式+注册表 / 函数式回调 / 管道链式
- [ ] 单算法激活 vs 多算法并行对比
- [ ] 三轴坐标系轴向映射确认
- [ ] CSV 列定义

## 四、进度

- 2026-07-17：需求整理，架构方案待用户拍板（本文件）。
- 2026-07-20：主菜单独立“位置测试”页面进入树莓派对接调试阶段。已确认：树莓派只运行 ROS 导航/SLAM 不会自动发送 `0xF5`；必须额外运行测试发送程序（如 `send_slam_cur_f5.py`）才会向 STM32 发当前位置。该设计是安全解耦：导航建图、测试发送、飞控解析、GUI显示分开验收，避免联调期误触发控制。
- 2026-07-20：`0xF5 -> STM32 -> 0xF6 -> 数传 -> GUI` 原始数据链路已验收。用户运行测试发送程序后，GUI 可以看到原始解析数据；此前“等待 0xF6”的主要原因是未运行发送程序，不是烧录失败，也不是 IMU/数传不能转发自定义帧。后续从 GUI 功能主线继续：坐标标定、稳定性测试、轨迹回放和调试诊断。
- 2026-07-20：GUI 主线已从烧录/链路排障恢复到“位置测试”功能开发。新增链路状态提示、稳定性页、坐标标定页、轨迹回放/CSV页；均为上位机诊断功能，不接 PID、不发 `0x41`、不改变飞控输出。离线验证截图：`artifacts/gui/position_test_waiting_hint.png`、`position_test_stability.png`、`position_test_calibration.png`、`position_test_trajectory.png`。
- 2026-07-20：根据现场截图做可用性完善。坐标标定页当前 cur 改为亮绿大字，新增用户级三步操作引导；稳定性页从被动窗口统计改为“开始测试/停止结算”的主动测试流程，默认测试类型为“静止定位稳定性（起飞前观测质量检查，不是飞行控制稳定性）”，并增加抖动σ、最大摆动、漂移、跳变和判读列。验证截图：`artifacts/gui/position_test_calibration_polished.png`、`position_test_stability_polished.png`。
- 2026-07-20：修复位置测试页面尺寸和频率口径问题。页面压缩说明文字、表格列宽和长状态文本，不再把主窗口撑出屏幕；`当前 cur` 和稳定性摘要仍保留亮绿色大字。实时页频率改为 `F5输入 / F6镜像`，用 `rx_cnt` 差值估算真实树莓派输入频率；固件 `0xF6` 调试镜像由约 10Hz 提到约 25Hz。离线注入验证显示 `50.0 / 25.0 Hz`，截图：`artifacts/gui/position_test_rate_compact_realtime.png`、`position_test_calibration_compact.png`、`position_test_stability_compact.png`。
- 2026-07-20：二次修正坐标标定/稳定性排版。坐标标定页加入整体滚动容器，点位表固定3行、结果表固定6行，避免结果栏被压成一行；稳定性绿色摘要改为一行纯数据，不再显示“点击开始测试后才采样”。验证截图：`artifacts/gui/position_test_calibration_layout_fixed.png`、`position_test_stability_layout_fixed_idle.png`、`position_test_stability_layout_fixed_running.png`。

## 五、当前主线恢复点（2026-07-20）

1. 已完成：推荐方案落地，不改树莓派已跑通的 `0xF5`，STM32新增标准匿名 `0xF6` 下行镜像帧，GUI新增“位置测试”入口和实时数据页。
2. 已完成：原始数据在 GUI 可见，当前阶段仍只显示/诊断，不接 PID、不发 `0x41`、不改变飞行输出。
3. 已完成离线版：实时数据页能解释“未连接/等待发送程序/有遥测无0xF6/实时/超时”；稳定性页统计均值、标准差、峰峰值、无效样本和 `rx_cnt` 跳变；坐标标定页支持 O/+X/+Y 三点采样并计算平面变换；轨迹页支持记录、XY 曲线、目标点和 CSV 导出。
4. 已完成可用性完善：稳定性页必须点击“开始测试”才采样，避免误把历史窗口当成测试结果；坐标标定页提供推荐步骤但不强制。
5. 已完成频率口径修正：GUI 不能再把 `0xF6` 下行镜像频率误当成树莓派 `0xF5` 输入频率；现场看频率时以 `F5输入 / F6镜像` 两个数分别判断。
6. 已完成坐标标定/稳定性排版二次修正：标定结果6行完整可见，页面可鼠标滚轮查看；稳定性摘要为一行纯数据。
7. 固定测试纪律：每一步只打开一个小目标；树莓派侧必须明确是否运行测试发送程序；GUI侧必须用截图/抓包验证，不凭感觉判定成功。
8. 下一步现场验收：用户运行树莓派测试发送程序后，在 GUI 依次检查实时数据、稳定性、坐标标定和轨迹 CSV；若字段正确，再把真实黑线移动数据发回用于判断坐标变换。
