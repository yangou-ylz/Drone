# 凌霄飞控项目记忆验收题目

> 共 40 道题，全部简答，不需要写长篇大论，能说清楚核心点即可。
> 这些题目覆盖飞控代码、开发阶段、GUI开发、历史踩坑四个方向。

---

## 第一部分：飞控代码理解（STM32 / MCU 固件）共 20 题

**Q1.** 用户飞行逻辑的唯一合法入口函数是哪个？它在哪个文件中？被调度器以多少 Hz 的频率调用？

**Q2.** 项目调度器共有几个频率循环？请列出所有频率（Hz），并举例说明哪类任务应该放在哪个频率。

**Q3.** `fc_sta.fc_mode_sta` 的四个取值分别代表什么飞行模式？程控自主飞行必须处于哪个值？

**Q4.** 遥控器 AUX1 通道（CH5）的值在 1200~1400 和 1600~1800 范围内时，代表什么含义？为什么这两个区间不对应任何正常模式？

**Q5.** 凌霄协议中，向 IMU 发送 CMD 命令应使用哪个目标地址？向上位机回复 CK 应使用哪个目标地址？读写凌霄 IMU 内部参数时又应该用哪个地址？

**Q6.** 请写出凌霄协议双向校验（SC/AC）的计算方式，包括循环范围（从第几个字节到第几个字节）。

**Q7.** 凌霄协议帧的完整结构是什么？请按顺序列出每个字段的名称和含义。

**Q8.** IMU 发给 STM32 的 `0x03` 帧和 `0x04` 帧分别携带什么数据？两者有什么区别？哪个优先级更高？

**Q9.** 为什么凌霄协议中所有通过 UART 传输的数据结构体都必须加 `__attribute__((__packed__))`？不加会出现什么问题？

**Q10.** `dt.wait_ck` 这个字段的作用是什么？发送 CMD 帧之前为什么必须检查它为 0？如果忽略这个检查会发生什么？

**Q11.** `pid_3d_task` 函数的 `axis_mode` 参数有哪几个值？每个值对应哪些轴的控制？数值 4 是什么含义？

**Q12.** 当前室内飞控的串口分配情况是什么？UART5、UART4、USART6 分别连接什么设备？

**Q13.** `0xA0` LOG打印帧的 DATA 区格式是什么？至少要包含哪几个字段？`LEN` 字段的值怎么计算？

**Q14.** `0x41` 实时控制帧的作用是什么？发送这个帧有什么前置条件？在什么模式下飞控才会响应它？

**Q15.** 上行指令帧 `0xF2` 的帧格式是什么？白名单 ID 有哪几个？分别对应什么参数？安全限幅是多少？

**Q16.** `0xF3` 帧与 `0xF2` 帧有什么关系？`0xF3` 帧的 DATA 区长度是多少字节？帧总长是多少字节？

**Q17.** 项目中规定，所有需要等待条件的逻辑必须用什么模式实现？为什么禁止在调度循环里用 `while` 死等？

**Q18.** B类 CMD 和 C类 CMD 各有什么使用限制？请各举一个 B 类和 C 类 CMD 的例子（CID/CMD0/CMD1值）。

**Q19.** `ANO_DT_LX.c` 中存在一个原始代码 bug，请描述这个 bug 的具体位置、内容和影响。

**Q20.** `LX_FC_EXT_Sensor.h` 中的 `_general_dis_st.distance_cm` 字段存在什么类型声明错误？这个错误在什么情况下会产生问题？

---

## 第二部分：阶段开发情况共 10 题

**Q21.** 当前飞控固件侧已完成了哪几个主要开发阶段？请按顺序列出（阶段0~阶段4+）并一句话说明每阶段做了什么。

**Q22.** PID 三轴联合任务的三个触发通道分别是哪个（CH几）？每个通道触发后执行什么轴的任务，目标坐标默认是多少？

**Q23.** `RC_IDENTIFY_SAFE_MODE` 这个宏的作用是什么？当它被设置为 1 时会发生什么？地面识别通道结束后必须做什么？

**Q24.** `PID3D_SCALE_Y` 当前的值是多少？为什么要从初始值修改到现在的值？修改的依据是什么实验数据？

**Q25.** `PID3D_VY_XCOUPLE_GAIN` 是什么？它的当前值是多少？这个参数补偿的是什么物理现象？

**Q26.** 0xF2/0xF3 帧的硬件验证结果是什么？哪些 case 通过了？实际测试的目标坐标是什么？

**Q27.** 为什么 `0x08` 位置帧不能用于 XY 轴闭环控制？当前 XY 轴使用什么作为反馈源（`PID3D_OBS_X/Y_MODE`=多少）？

**Q28.** `MAX_VELOCITY` 被从原始值改为多少？死区（deadzone）从多少改到多少？这些修改是为了解决什么问题？

**Q29.** 上行命令链路的稳定性测试结果是什么？为什么 10Hz × 30s 只有 19% 通过率？实际使用建议的发送频率是多少？

**Q30.** 当前项目中有哪些功能或问题被"冻结"（明确不再在当前阶段修复），原因是什么？

---

## 第三部分：GUI 上位机开发共 5 题

**Q31.** GUI 上位机的三层架构分别是什么？每层包含哪些主要模块？各层之间通过什么方式通信？

**Q32.** `FakeWorker` 是什么？它和 `SerialWorker` 的关系是什么？如何激活 Fake 模式进行离线测试？

**Q33.** 路径可视化大阶段 P0~P10 总共完成了什么功能？P5 的"定向重建"机制是什么，为什么不直接重建整个场景？

**Q34.** 为什么 IMU `0x07` 速度帧的 XY 位移积分存在根本性的漂移问题？用 GUI 端的算法能不能根治这个问题？正确的解决方向是什么？

**Q35.** `AckMatcher` 的 token 机制是怎么工作的？它解决了什么并发问题？为什么必须"先登记后发送"？

---

## 第四部分：历史踩坑记录共 5 题

**Q36.** 有一次烧录了新代码，但飞控行为完全没有改变，像是新代码没有生效。根本原因是什么？以后遇到"功能不生效"时第一个要排查什么？

**Q37.** 在 Y 轴 PID 任务初期，日志里始终看不到 `vel_y` 输出，Y 轴完全没有响应。根本原因是什么？这个教训揭示了什么架构问题？

**Q38.** 在 GUI 开发中，点击"断开连接"按钮没有任何反应，串口线程无法停止。这个问题的根本原因是什么？涉及 Win32 API 的哪个调用遗漏了？

**Q39.** 在真机测试中，CW90 旋转停下后，GUI 里的立方体会慢慢自己逆转约 100° 以上。请解释这个现象的**两个**根本原因（一个来自 IMU，一个来自 GUI 算法）。

**Q40.** 在用 PowerShell 向 `dev.md` 追加中文内容时，出现了乱码。原因是什么？正确的追加方式应该用什么命令或参数？

---

## 第五部分：飞控代码理解进阶（STM32 / MCU 固件）共 20 题

**Q41.** `0x05` 高度帧的 DATA 区包含哪几个字段？`ALT_FU` 和 `ALT_ADD` 分别代表什么？单位是什么？路径可视化中 Z 轴使用的是哪个字段，为什么？

**Q42.** `0x06` 飞控状态帧的 DATA 区有哪些字段？其中 `MODE`、`LOCKED`、`CID`、`CMD0`、`CMD1` 各自的含义是什么？

**Q43.** `0x07` 飞行速度帧的速度数据是机体坐标系还是世界坐标系？机体坐标系的 FLU 是什么意思？`vx` 方向为什么在实际代码里需要取负号？

**Q44.** `0x34` 测距帧的 DATA 区结构是什么？`DIRECTION`、`ANGLE`、`DIST` 三个字段各自的类型和单位是什么？无效值如何表示？

**Q45.** `0x32` 通用位置帧和 `0x33` 通用速度帧的 DATA 区分别是什么格式？LEN 各是多少？无效值分别用什么表示？

**Q46.** `0xE0` CMD 帧的 DATA 区结构是什么？`LEN` 固定为多少？`CID`、`CMD0`、`CMD1` 字段在 DATA 中的位置是什么？

**Q47.** 硬件地址 `0x22` 和 `0x30` 分别代表什么设备？当前室内机配置中是否实际使用了这两个地址对应的设备？

**Q48.** `SysConfig.h` 中定义了哪些基本类型别名？请列出 `u8`、`s16`、`s32`、`u32` 对应的标准 C 类型。为什么要用这些别名而不直接用 `int`？

**Q49.** 项目中实现了两种 `0xA0` 日志发送方案：UART2 直连方案和 UART5→IMU 方案。请分别说明各方案调用的函数名、数据路径和主要优缺点。

**Q50.** `0x04` 四元数帧解码后得到的 `yaw_deg` 为什么需要取负号？不取负时会出现什么错误现象？这个问题是在哪个文件的哪个函数里修复的？

**Q51.** `LX_FC_Fun.h` 提供了哪些核心 API？请至少列出解锁、上锁、一键起飞、切换模式四类函数的函数名（或功能描述）。

**Q52.** 项目的编译开关体系中，`PID3D_EN` 和 `PID_TEST_EN` 这两个宏分别控制什么代码的编译？两者必须同时为 1 才能让 `pid_3d_task` 编译进固件吗？

**Q53.** `0xA0` LOG 帧的"颜色"字段有哪几个常用枚举值？绿色、红色、黑色分别对应什么数值？匿名上位机里颜色显示的意义是什么？

**Q54.** `Uplink_Cmd.c` 中 `Uplink_Cmd_Tick()` 函数的作用是什么？它以多少 Hz 调用？它处理的 ECHO 队列里有哪几种 KIND？

**Q55.** STM32F407 的系统时钟是多少 MHz？APB1 和 APB2 总线频率分别是多少？APB1 上的定时器时钟频率实际是多少，为什么不是 42MHz？

**Q56.** `PID3D_VEL_TOTAL_CMPS` 是什么参数？它的值是多少？当 X+Y 联动（axis_mode=4）时，X 和 Y 方向各自的实际速度上限大约是多少？计算依据是什么？

**Q57.** 项目禁止使用动态内存（`malloc`/`free`），函数内部的大型局部数组（超过 256 字节）应如何声明？原因是什么？

**Q58.** Keil5 工程文件的完整相对路径是什么？编译输出目录在哪里？如何通过脚本清除编译产物？

**Q59.** `ANO_DT_LX_Data_Receive_Prepare()` 状态机中，状态1 是怎么过滤目标地址的？哪两个地址会被接受，哪些地址会被丢弃？

**Q60.** 向 IMU 发送 CMD 后，`dt.wait_ck` 会被置为非零。在 CK（确认帧）返回之前，可以再发一个新的 CMD 吗？CK 返回后 `dt.wait_ck` 如何变化？

---

## 第六部分：阶段开发情况进阶共 10 题

**Q61.** `wave/` 目录下的波形分析工具有哪 8 个分析模块？请逐一说明每个模块分析什么内容。

**Q62.** 实飞波形分析中发现了 19.3Hz 的振动峰值，这是什么原因造成的？建议的解决方案是什么（已实施还是待实施）？

**Q63.** 阶段 0.5 中从官方手册里发现了哪些初始配置错误？请列出至少 4 个被修正的具体问题。

**Q64.** `Uplink_Cmd.h/.c` 的完整架构是什么？请说明 `Init`、`Tick`、`Dispatch`、`Send_Ack` 四个函数各自的职责。

**Q65.** `0xF1` 灵活帧的地面验证结果是什么？单帧通过率是多少？2Hz × 30s 的稳定性测试通过率是多少？

**Q66.** 阶段 0.6 中新增的 `String_Info_Send` 函数最终将目标地址从 `0xAF` 改为了 `0xFF`，原因是什么？这涉及到对官方手册哪个原则的核查？

**Q67.** 上行命令链路文档体系（阶段5）中，`数据帧.md` 文件的主要内容是什么？为什么明确规定 CMD `0xE2` 禁止使用？

**Q68.** Y 轴 PID 任务实飞后发现超调约 59%，具体表现是什么（目标多少，实飞多少）？当前的临时缓解方案是什么，根本原因有没有消除？

**Q69.** `groundTest/` 目录下有哪些地面测试工具脚本？请分别说明 `send_f1.py`、`send_param.py`、`send_xyz.py`、`monitor.py` 各自的用途。

**Q70.** `0xF2` 帧 `param_apply()` 函数的生效时机是什么？飞行过程中能不能实时修改目标坐标？修改后需要怎样操作才能让新参数生效？

---

## 第七部分：GUI 上位机开发进阶共 5 题

**Q71.** `ConfigService` 使用白名单设计（`_DEFAULTS` 字典）。如果向 config 写入了一个未在 `_DEFAULTS` 中登记的键，会发生什么？每次新增持久化键时必须同时做什么？

**Q72.** P8 阶段实现了 K 段路径渲染。为什么不能用单条 `GLLinePlotItem` 的逐点颜色来实现"近粗近亮远细远淡"？K 段方案的核心数据结构是什么？段与段之间如何保证视觉不断节？

**Q73.** `gui/services/frame_recorder.py` 的 `FrameRecorder` 记录哪些帧 ID？JSONL 文件每行的格式是什么？为什么需要 `_meta` 头尾标记？

**Q74.** `gui/test/` 目录下有哪些 smoke 测试文件？请说明 `_smoke_phase_p6.py` 验证了哪 5 个 case（节流限流相关）。

**Q75.** `PathTracker` 中 `enable()` 时会记录 `_z_offset_cm`，其值来源于什么？这个设计解决了什么用户体验问题？如果不做这个偏移会出现什么现象？

---

## 第八部分：历史踩坑进阶共 5 题

**Q76.** GUI 开发中，点击"连接"按钮串口死活连不上，`QThread` 的 worker 线程确实在跑，但槽函数不执行。根本原因是什么？修复方法是什么？

**Q77.** 在 Z 轴 PID 任务早期，Z 轴任务激活时油门被错误地固定为 500，导致无法正常起飞/控高。根本原因是什么变量的混用？正确的拆分方案是什么？

**Q78.** 在 GUI 开发中，用 `replace_string_in_file` 重构某个方法时，操作完成后运行提示 `SyntaxError`。排查后发现函数体变成了空函数。这个错误是怎么发生的？以后重构方法体应该遵守什么原则？

**Q79.** 2D 导航视图里，纸飞机图标怎么都看不见，但代码里已经正确计算了位置和形状。根本原因是什么？`pyqtgraph` 里 `plot()` 的 `fillLevel` 参数有什么坑？正确的实现应该用什么类？

**Q80.** `PySide6` 跨线程调用时，用 `Q_ARG(bytes, frame)` 传递数据，运行时报 `qArgDataFromPyType: Unable to find a QMetaType for "bytes"`。原因是什么？发送端和接收端分别应该如何修改？
