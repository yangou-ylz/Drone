# 上行自定义数据帧

匿名协议基础格式：`AA | dest | CMD | LEN | DATA[LEN] | SC | AC`
校验：`for i in 0..LEN+3: SC+=buf[i]; AC+=SC`（皆 u8）

---

## 通信链路

```
PC (send_param.py / send_f1.py)
   │  Win32 CreateFile（500000 baud，驱动固化）
   ▼
匿名数传 USB-CDC（COM11）
   │  2.4G 无线
   ▼
凌霄 IMU（地址 0x60）
   │  UART 透传 dest=0xFF/0x61 的帧
   ▼
STM32F407 飞控
   │  Drv_Uart RX → ANO_DT_LX Anl() 状态机 → SC/AC 校验
   ▼
Uplink_Cmd_Dispatch(data, len)
   ├── CMD=0xF1 → 阶段1 链路回显
   ├── CMD=0xF2 → 阶段2 写 s_goal_x/y/z_cm 之一
   ├── CMD=0xF3 → 阶段2b 同帧写 s_goal_x/y/z_cm 全部三轴
   ├── CMD=0xF5 → 阶段3a 树莓派位置帧快照（只解析/日志，不控飞）
   └── CMD=0xF7 → 阶段6 GUI自主任务命令（状态机触发）
   ▼
Uplink_Cmd_Tick() @50Hz
   │  组装 0xA0 字符串帧；必要时把0xF5快照镜像成0xF6
Auto_Mission_Tick_50Hz()
   │  执行自主任务状态机；必要时下发0xF8状态帧
   ▼
String_Info_Send → ANO_DT_LX 发送队列 → UART → IMU → 2.4G → PC
   ▼
FrameParser → 终端打印 [RX 0xA0 GREEN/RED] xxx
```

### UART2 树莓派链路硬件前置检查（2026-07-24纠错）

在测试 `0xF5/0xF6` 或外部传感器前，先确认 `0x0D` 电池电压帧正常。现场已确认：某款 ANO `SWD&UART V2.0` 类 DAP/UART 复合模块插到树莓派 USB 后，即使树莓派关机，也会导致飞控电压数据归零/消失，随后光流、激光高度、`0x33/0x34` 和 `0x0E` 外部传感状态消失；插入时出现 `[A0 红] 运动解算失效复位`，拔掉后出现 `[A0 绿] 运动解算启动`。

因此树莓派 UART2 串口桥优先使用已验证的简单 3.3V USB-TTL，只接 `TX/RX/GND`，`VCC/5V` 不接。每次更换串口模块后，先验证 `0x0D` 电压和 `0x0E` 外接状态稳定，再继续协议和坐标测试。

### USART5 只读旁路监听规则（2026-07-24新增纠错）

`UART5/USART5` 是 STM32 ↔ 凌霄 IMU 主通信口，不是树莓派可主动发送的调试口。板上 `RX/TX` 标注相对于 STM32：`USART5 RX` 是 STM32 接收 IMU 数据（IMU→STM32），`USART5 TX` 是 STM32 发送给 IMU（STM32→IMU）。

如果树莓派只为了雷达点云建图读取 IMU 的姿态、速度、高度、电压/状态等数据，只能旁路监听：`飞控USART5 RX → USB-TTL RX`、`GND ↔ GND`，USB-TTL `TX`、`VCC/5V` 和飞控 `USART5 TX` 都不接。这个接法看似 RX 接 RX，但本质是高阻监听已有的 IMU→STM32 数据线。

一旦把 USB-TTL `TX` 接入 USART5 主通信总线，现场已实测会导致 `0x0D` 电压、光流、激光高度、通用速度等数据瞬间异常。该问题与 UART2 DAP/UART 串口桥反灌/电平问题是两个独立原因，不能互相覆盖。

### 自主运行当前阶段约定（2026-07-26）

方案 A 已进入实施：**STM32 主导自主任务状态机，GUI 只触发，树莓派只供位置**。但当前低高度自主起降阶段先不等待树莓派雷达/SLAM彻底调好；飞控侧先沿用已经实测误差较小的内部速度积分/现有位置观测路径，把 `0xF5` 保留为后续位置观测接口，不作为起飞/降落预检硬依赖。

第一版自主起降只验证 `GUI 0xF7 → STM32状态机 → 凌霄IMU官方一键指令`，不写 `rt_tar.vel_x/y/z`，不触发 `0x41` 水平速度控制，不接 XY 位置 PID。

---

## 0xF1 — 灵活帧（阶段1，链路验证）

| 字段 | 字节 | 值 |
|---|---|---|
| 帧头 | 1 | `0xAA` |
| 地址 | 1 | `0xFF` 广播（推荐）或 `0x61` STM32 |
| CMD | 1 | `0xF1` |
| LEN | 1 | `0x04` |
| X | 2 | s16 小端 |
| Y | 2 | s16 小端 |
| SC/AC | 2 | 校验 |

**示例**：`AA FF F1 04 D2 04 2E EE 90 A1` → X=1234, Y=-4562
**飞控行为**：仅记录 + 回显，**不改任何状态**
**回显**：? `F1: X=1234 Y=-4562`

---

## 0xF2 — 参数写入帧（阶段2，PID3D 目标坐标）

| 字段 | 字节 | 值 |
|---|---|---|
| 帧头 | 1 | `0xAA` |
| 地址 | 1 | `0xFF` |
| CMD | 1 | `0xF2` |
| LEN | 1 | `0x05` |
| param_id | 1 | 白名单见下 |
| value | 4 | IEEE754 float 小端，单位 cm |
| SC/AC | 2 | 校验 |

### 参数白名单

| param_id | 写入目标 | 范围 |
|---|---|---|
| `0x01` | `s_goal_x_cm` → PID3D GOAL_X | ±500 cm（越界 clamp）|
| `0x02` | `s_goal_y_cm` → PID3D GOAL_Y | ±500 cm |
| `0x03` | `s_goal_z_cm` → PID3D GOAL_Z | ±500 cm |
| 其他 | — | 拒绝（红字 UNK）|

**示例**：`AA FF F2 05 01 00 00 F0 41 D2 D4` → 设 GOAL_X = 30.0 cm

**生效时机**：仅写入 RAM。PID3D 任务在 step=1 启动时通过 Getter 拍照锁定为 const，飞行中不变。需 CH6 拨回再拨回触发任务重启才使用新值。**断电丢值**。

### 回显

| 终端显示 | 含义 |
|---|---|
| ? `P01=30.0` | 写入成功 |
| ? `P01=500.0 CLP` | 越界，已限幅 |
| ? `P09 UNK` | param_id 不在白名单 |

---

## 0xF3 — 三轴目标同帧写入帧（阶段2b，原子批量写 X+Y+Z）

| 字段 | 字节 | 值 |
|---|---|---|
| 帧头 | 1 | `0xAA` |
| 地址 | 1 | `0xFF` |
| CMD | 1 | `0xF3` |
| LEN | 1 | `0x0C`（= 12） |
| x | 4 | IEEE754 float 小端，单位 cm |
| y | 4 | IEEE754 float 小端，单位 cm |
| z | 4 | IEEE754 float 小端，单位 cm |
| SC/AC | 2 | 校验 |

**总长 18B**（4B帧头/地址/CMD/LEN + 12B数据 + 2B校验）。三轴各自独立限幅 ±500cm，任一轴被限幅 → 回显末尾带 `CLP`。

**示例**：`AA FF F3 0C 00 00 F0 41 00 00 30 42 00 00 5C 42 SC AC` → X=30.0, Y=44.0, Z=55.0

**与 0xF2 的关系**：共享同一组 RAM 槽位与同样的限幅逻辑，生效时机一致（任务启动拍照）。难以用 0xF2 原子写完三轴（需 3 帧串联，任一丢包则状态擕裂）；**三轴同发选 0xF3**，单轴微调选 0xF2。

### 回显

| 终端显示 | 含义 |
|---|---|
| ? `P*=30.0,44.0,55.0` | 三轴写入成功 |
| ? `P*=500.0,44.0,55.0 CLP` | 任一轴被限幅 |

不会出现 `UNK`（0xF3 无 ID 字段）。

**生效时机 / 断电丢值**：与 0xF2 完全一致。

---

## 0xF5 — 树莓派位置帧（阶段3a，只收不控飞）

| 字段 | 字节 | 值 |
|---|---|---|
| 帧头 | 1 | `0xAA` |
| 地址 | 1 | `0x61` STM32 飞控（广播 `0xFF` 也可被接收） |
| CMD | 1 | `0xF5` |
| LEN | 1 | `0x19`（= 25） |
| cur_x | 4 | s32 小端，当前位置 X，cm |
| cur_y | 4 | s32 小端，当前位置 Y，cm |
| cur_z | 4 | s32 小端，当前位置 Z，cm |
| tar_x | 4 | s32 小端，目标 X，cm |
| tar_y | 4 | s32 小端，目标 Y，cm |
| tar_z | 4 | s32 小端，目标 Z，cm |
| flags | 1 | bit0=SLAM_VALID, bit1=TARGET_VALID, bit2=VISUAL_MODE |
| SC/AC | 2 | 校验 |

**总长 31B**。无效 s32 轴的协议字节为 `0x80000000`，Python signed int 必须用 `-2147483648` 打包，不能把 `0x80000000` 直接传给 `struct.pack("<i")`。

**黄金样例**：`cur=(0,0,80), tar=(100,0,80), flags=0x03`

```text
AA 61 F5 19 00 00 00 00 00 00 00 00 50 00 00 00 64 00 00 00 00 00 00 00 50 00 00 00 03 20 36
```

**飞控当前行为**（2026-07-19 阶段3a）：UART2/PD6 接收，解析 `cur/tar/flags` 并保存最近一帧快照；每个合法帧通过 UART2/PD5 回 `0xA0` 绿色 ACK：

```text
F5 #12 f=03 c=0,0,80 t=100,0,80
```

当前阶段 **不接 PID、不写 rt_tar、不触发 0x41、不改变飞行输出**。后续必须先完成串口、坐标方向、失效/超时测试，再进入 PID 干运行和实机低速单轴测试。

---

## 0xF6 — 树莓派位置镜像调试帧（STM32 → IMU → 数传 → GUI）

`0xF6` 是为了 GUI“位置测试”新增的标准匿名自定义下行帧。它不替代、不修改树莓派已经跑通的 `0xF5`；STM32 仍按 `0xF5` 接收树莓派数据，只是在解析成功后把最近快照重新打包，经 UART5 发给凌霄 IMU，再由 IMU/数传转发到 GUI。

| 字段 | 字节 | 值 |
|---|---:|---|
| 帧头 | 1 | `0xAA` |
| 地址 | 1 | `0xFF` 广播 |
| CMD | 1 | `0xF6` |
| LEN | 1 | `0x25`（= 37） |
| cur_x | 4 | s32 小端，当前位置 X，cm |
| cur_y | 4 | s32 小端，当前位置 Y，cm |
| cur_z | 4 | s32 小端，当前位置 Z，cm |
| tar_x | 4 | s32 小端，目标 X，cm |
| tar_y | 4 | s32 小端，目标 Y，cm |
| tar_z | 4 | s32 小端，目标 Z，cm |
| flags | 1 | 与 `0xF5` 相同：bit0=SLAM_VALID, bit1=TARGET_VALID, bit2=VISUAL_MODE |
| rx_cnt | 4 | u32 小端，合法 `0xF5` 累计数 |
| len_err_cnt | 4 | u32 小端，`0xF5` 长度错误累计数 |
| checksum_err_cnt | 4 | u32 小端，`0xF5` 校验错误累计数 |
| SC/AC | 2 | 校验 |

**总长 43B**（4B 头区 + 37B DATA + 2B 校验），小于当前 `send_buffer[50]`。

**发送节流**：`Uplink_Cmd_Tick()` 约 50Hz 运行，`UPLINK_F6_MIRROR_TICK_GAP=2`，因此 `0xF6` 镜像最高约 25Hz；只有收到新的合法 `0xF5` 后才发送，不会在无新数据时反复刷屏。

**GUI 行为**：主菜单 `功能 → 位置测试` 打开后才解析和刷新 `0xF6`；页面关闭或切走后停止自己的刷新定时器。当前第一版只做“实时数据”页，坐标标定、稳定性和轨迹回放是下一阶段占位，未接控制输出。

---

## 0xF7 — GUI 自主任务命令帧（GUI → STM32）

`0xF7` 用于方案 A 自主任务状态机。GUI 只发触发/中止/急停命令；STM32 在定点模式 Mode2 内执行状态机；树莓派 `0xF5` 不被修改，也不是当前低高度起降阶段的硬依赖。

固定帧格式：

| 字段 | 字节 | 值 |
|---|---:|---|
| 帧头 | 1 | `0xAA` |
| 地址 | 1 | `0xFF` 广播 |
| CMD | 1 | `0xF7` |
| LEN | 1 | `0x10`（= 16） |
| ver | 1 | u8，当前 `1` |
| seq | 2 | u16 小端，GUI自增序号 |
| cmd | 1 | 命令字，见下表 |
| safety_key | 2 | u16 小端，运动相关命令、释放遥控权、锁定遥控权命令必须为 `0xA55A` |
| height_cm | 2 | u16 小端，第一版允许 `30~80cm`，默认 `40` |
| hold_ms | 2 | u16 小端，第一版允许 `1000~5000ms`，默认 `3000` |
| flags | 2 | u16 小端，第一版强制 `bit3 no_xy_motion=1` |
| timeout_ms | 2 | u16 小端，默认 `30000` |
| reserved | 2 | u16 小端，保留为 `0` |
| SC/AC | 2 | 校验 |

命令字：

| cmd | 含义 | safety_key |
|---:|---|---|
| `0x00` | 查询状态 | 不需要 |
| `0x01` | 预检 | 不需要 |
| `0x02` | 请求定点模式 Mode2 | 需要 |
| `0x03` | 起降状态机干运行 | 需要 |
| `0x04` | 正式低高度起飞-悬停-降落-上锁 | 需要 |
| `0x05` | 中止并降落 | 不需要 |
| `0x06` | 强制上锁/急停 | 不需要，最高优先级 |
| `0x07` | 清错误回空闲 | 不需要 |
| `0x08` | 释放遥控权，允许传统遥控器重新接管模式/摇杆/CH6任务 | 需要 |
| `0x09` | 锁定遥控权，重新禁止传统遥控器写模式/摇杆/CH6任务 | 需要 |

第一版实现的安全边界：

- 正式起降只调用 `LX_Change_Mode(2)`、`FC_Unlock()`、`OneKey_Takeoff(height_cm)`、`OneKey_Land()`、`FC_Lock()`。
- 不写 `rt_tar.vel_x/y/z`，不触发 `0x41` 水平速度控制。
- `EMERGENCY_LOCK` 会清零 `rt_tar` 并持续尝试 `FC_Lock()`。
- `ABORT_LAND` 是常规中止：先发 `OneKey_Land()`，进入 `WAIT_LAND`，随后请求 `FC_Lock()`；状态机必须等 `unlock=0` 才能进入 `DONE`。这和 `EMERGENCY_LOCK` 的立即上锁不同。
- 低高度起降必须满足电压有效、上锁状态开始、`dt.wait_ck==0`、`0x0E` 通用速度 `G_VEL>=2`、附加测高 `ALT_ADD>=2`，Mode2稳定后才继续。
- 遥控器默认被 AUTO 控制权锁住：RC 不切模式、不写 `rt_tar`、不触发摇杆解锁/上锁、也不触发旧的 CH6/CH7/CH10 测试任务；同时 STM32 发给 IMU 的 `0x40` 会被替换成虚拟中位遥控，避免真实油门低位/摇杆非中位压住 Mode2 一键起飞。只有 GUI 发 `cmd=0x08` 且 `safety_key=0xA55A` 后，RC 才能恢复真实遥控透传和干预。GUI 发 `cmd=0x09` 且 `safety_key=0xA55A` 后，重新回到 AUTO 锁定。比赛阶段不要释放 RC。
- 正式起飞不再只按固定时间进入悬停：进入 `TAKEOFF_REQ` 前记录 `0x05 alt_fu_cm` 为基准，只按相对高度增量判断离地。高度增量连续达到阈值后才进入 `HOLD`；若超时仍未离地，报 `0x0041 起飞未离地` 并执行中止降落。

黄金样例：`seq=1, cmd=PRECHECK, height=40cm, hold=3000ms, flags=0x0008, timeout=30000ms`

```text
AA FF F7 10 01 01 00 01 00 00 28 00 B8 0B 08 00 30 75 00 00 4B BB
```

---

## 0xF8 — 自主任务状态帧（STM32 → IMU → 数传 → GUI）

`0xF8` 是 `0xF7` 状态机的结构化下行状态。GUI 用它显示/记录状态变化；`0xA0` 文本日志继续保留给人工排查。

| 字段 | 字节 | 值 |
|---|---:|---|
| 帧头 | 1 | `0xAA` |
| 地址 | 1 | `0xFF` 广播 |
| CMD | 1 | `0xF8` |
| LEN | 1 | `0x19`（= 25） |
| ver | 1 | u8，当前 `1` |
| status_seq | 2 | u16 小端，状态帧序号 |
| last_cmd_seq | 2 | u16 小端，最近一次 `0xF7 seq` |
| state | 1 | 状态机状态 |
| last_cmd | 1 | 最近一次 `0xF7 cmd` |
| error | 2 | 错误码，`0=OK` |
| flags | 2 | 状态标志 |
| mode | 1 | `fc_sta.fc_mode_sta` |
| unlock | 1 | `fc_sta.unlock_sta` |
| voltage_100 | 2 | 电压×100 |
| alt_cm | 2 | s16，高度cm，来自IMU `0x05 alt_fu`缓存 |
| state_ms | 2 | 当前状态停留时间ms |
| f5_age_ms | 2 | 最近 `0xF5` 年龄；无数据为 `65535` |
| rx_f7_cnt | 2 | 合法 `0xF7` 累计数 |
| err_cnt | 2 | 自主状态机错误累计数 |
| SC/AC | 2 | 校验 |

`flags`：

| bit | 含义 |
|---:|---|
| 0 | 电压有效 |
| 1 | 当前 Mode2 |
| 2 | 已解锁 |
| 3 | no_xy_motion 强制开启 |
| 4 | `0xF5` 新鲜（当前阈值 500ms） |
| 5 | 状态机处于活动状态 |
| 6 | `0x0E` 通用速度状态正常/良好（`STA_G_VEL>=2`） |
| 7 | `0x0E` 附加测高状态正常/良好（`STA_ALT_ADD>=2`） |
| 8 | RC 被 AUTO 控制权锁住，遥控器不能写模式/速度/一键任务 |
| 9 | `rc_in.fail_safe!=0`，接收机进入失控保护，或飞控按 CH5 失控规则判定为失控 |
| 10 | `rc_in.no_signal!=0`，飞控 1s 内没有收到足够有效 PPM/SBUS 帧 |
| 11 | `rc_in.hold_frame!=0`，SBUS连续约1.5~2s输出完全相同的保持帧，疑似发射机关机但接收机仍在hold输出 |

RC状态判读不要混淆：

- `bit8=1` 只表示 **AUTO 锁住 RC 控制权**，即使接收机还在发帧，遥控器也不能改模式/写速度/触发旧任务。
- `bit10=1` 才表示飞控认为 **没有有效接收机帧**。
- `bit11=1` 表示接收机仍在发帧，但帧内容长时间完全不变；GUI 显示“保持帧疑似关机”，用于解决“发射机关机后接收机仍输出hold帧”的现场问题。
- 如果 GUI 显示“有效遥控帧”，它只说明 SBUS/PPM 接收机正在输出非失控、非保持帧；是否允许遥控器干预仍以 `RC控制权=AUTO锁定/RC可控` 为准。

发送频率：空闲约 2Hz；活动/任务中约 10Hz；状态变化时立即打一帧。GUI 主日志只记录关键字段变化，避免周期帧刷屏。

---

## 0xA0 — 字符串回显帧（飞控 → PC，已有协议）

由 `String_Info_Send(target, color, str)` 发出，用于飞控向上位机送 ASCII 文本。

| 字段 | 字节 | 值 |
|---|---|---|
| 帧头 | 1 | `0xAA` |
| 地址 | 1 | `0xAF`（指向上位机） |
| CMD | 1 | `0xA0` |
| LEN | 1 | N+2 |
| target | 1 | `0xFF` 全部 |
| color | 1 | 0=黑 1=红 2=绿 |
| ASCII | N | 不带 `\0` |
| SC/AC | 2 | 校验 |

本项目用法：F1/F2 命中后由 `Uplink_Cmd_Tick()` @50Hz 异步组帧（限频 10Hz 避免刷屏）。

---

## 地址速记

| 地址 | 含义 |
|---|---|
| `0xFF` | 广播 |
| `0x60` | 凌霄 IMU |
| `0x61` | STM32 飞控 |
| `0xAF` | 上位机 / PC |

## CMD 私有空间

`0xFx` 系列在凌霄官方协议中未使用，本项目占用：

| CMD | 用途 | 方向 |
|---|---|---|
| `0xF1` | 链路验证灵活帧 | PC → 飞控 |
| `0xF2` | 参数写入（单轴） | PC → 飞控 |
| `0xF3` | 三轴目标同帧写入 | PC → 飞控 |
| `0xF4` | 预留 | — |
| `0xF5` | 树莓派位置帧（cur/tar/flags，阶段3a只解析/日志） | 树莓派 → 飞控 |
| `0xF6` | 树莓派位置镜像调试帧（cur/tar/flags/计数） | STM32 → IMU → 数传 → GUI |
| `0xF7` | GUI 自主任务命令帧 | GUI → 飞控 |
| `0xF8` | 自主任务状态帧 | STM32 → IMU → 数传 → GUI |
| `0xF9`~`0xFF` | 预留 | — |

不要用 `0xE2`：被凌霄 CK_Back 协议占用，会触发 ACK 冲突。
